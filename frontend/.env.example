# API Base URL for backend server
# In Local Development: leave empty or use /api (Vite proxy will route to http://localhost:5000)
# In Production (Vercel): Set to your deployed backend URL + /api (e.g. https://your-backend.onrender.com/api)
VITE_API_URL=https://your-backend.onrender.com/api
